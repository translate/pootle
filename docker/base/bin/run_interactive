#!/bin/bash

# Prepares then runs the webapp.

cd /home/pootle/pootle_env \
      && /bin/bash -c "bash --rcfile <(echo \"\
           cd $INSTALL_DIR \
           && . bin/activate \
           && cd src/pootle \")"
